---
phase: quick
plan: 260321-s6x
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bagels/models/database/app.py
  - tests/integration/test_custom_root_session_rebind.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "`bagels --at <path>` rebinds pre-imported manager Session factories to the active database engine"
    - "Launching `uv run bagels --at ./instance` no longer crashes with `no such column: account.slug`"
    - "A regression test covers the stale Session binding scenario"
  artifacts:
    - path: "src/bagels/models/database/app.py"
      provides: "engine reconnect now rebinds loaded manager Session factories"
    - path: "tests/integration/test_custom_root_session_rebind.py"
      provides: "regression coverage for custom-root session rebinding"
  key_links:
    - from: "src/bagels/models/database/app.py"
      to: "bagels.managers.accounts.Session"
      via: "_rebind_loaded_sessionmakers() updates loaded manager sessionmakers"
      pattern: "_rebind_loaded_sessionmakers"
---

<objective>
Fix startup failure under `--at` where manager queries still point at the default database, causing schema mismatch errors like `no such column: account.slug`.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Rebind loaded manager Session factories when engine changes</name>
  <files>src/bagels/models/database/app.py</files>
  <action>
Add a helper that scans already-imported manager modules and reconfigures their `Session` factories to the new engine during `_ensure_engine_current()`.
  </action>
  <verify>
    <automated>uv run python - <<'PY'\nimport bagels.managers.accounts as accounts\nfrom bagels.locations import set_custom_root, database_file\nfrom bagels.models.database.app import init_db\nset_custom_root('./instance')\ninit_db()\nprint(accounts.Session.kw['bind'].url)\nprint(database_file())\nprint(accounts.get_accounts_count(get_hidden=True))\nPY</automated>
  </verify>
  <done>Manager Session binds point to the custom-root DB after init and account queries run without OperationalError.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression test for pre-import + --at custom root flow</name>
  <files>tests/integration/test_custom_root_session_rebind.py</files>
  <action>
Add an integration test that imports manager module first, then switches custom root and runs init_db, asserting Session bind URL points to tmp DB and query succeeds.
  </action>
  <verify>
    <automated>uv run pytest tests/integration/test_custom_root_session_rebind.py -q</automated>
  </verify>
  <done>Regression test fails before fix and passes after fix.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-run app with --at path</name>
  <files></files>
  <action>
Run `uv run bagels --at ./instance` briefly and confirm UI starts without the `account.slug` traceback.
  </action>
  <verify>
    <automated>Manual smoke run (start app, verify Home screen renders, quit via Ctrl+Q)</automated>
  </verify>
  <done>App starts successfully under custom root without schema column crash.</done>
</task>

</tasks>
