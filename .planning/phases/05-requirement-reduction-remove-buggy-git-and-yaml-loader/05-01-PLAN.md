---
phase: 05-requirement-reduction-remove-buggy-git-and-yaml-loader
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bagels/__main__.py
  - src/bagels/app.py
  - src/bagels/config.py
  - src/bagels/cli/__init__.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "`bagels git`, `bagels export`, and `bagels import` are no longer registered commands in the top-level CLI"
    - "Startup no longer runs YAML pull/import sync work from `App.on_mount()`"
    - "A one-time deprecation warning is emitted when legacy `git.*` config settings are present"
  artifacts:
    - path: "src/bagels/__main__.py"
      provides: "CLI command registry without git/export/import command wiring"
      min_lines: 60
    - path: "src/bagels/app.py"
      provides: "Startup flow without `run_startup_import` YAML sync invocation"
      min_lines: 80
    - path: "src/bagels/config.py"
      provides: "One-time deprecation warning + persisted state flag for legacy git/yaml sync config"
      min_lines: 220
  key_links:
    - from: "src/bagels/__main__.py"
      to: "src/bagels/cli/git.py"
      via: "command import and `cli.add_command(..., name='git')` removed"
    - from: "src/bagels/app.py"
      to: "src/bagels/importer/importer.py::run_full_import"
      via: "startup import path detached from runtime mount flow"
---

<objective>
Remove user-facing sync entrypoints and startup sync behavior so Bagels boots and runs in SQLite-only mode. Preserve regular CLI/TUI behavior and add exactly one compatibility warning path for legacy git/yaml sync config keys.
</objective>

<execution_context>
@/Users/thepbordin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/thepbordin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-requirement-reduction-remove-buggy-git-and-yaml-loader/05-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="code">
  <name>Remove sync command registration from top-level CLI</name>
  <files>src/bagels/__main__.py, src/bagels/cli/__init__.py</files>
  <read_first>
    <file>src/bagels/__main__.py</file>
    <file>src/bagels/cli/__init__.py</file>
  </read_first>
  <action>
    Delete imports and `cli.add_command(...)` registrations for `git`, `export`, and `import` commands.
    Keep existing query/read commands (`records`, `summary`, `accounts`, `categories`, `spending`, `trends`, `llm`, `schema`) unchanged.
    Ensure no stale re-export/import alias remains in `src/bagels/cli/__init__.py`.
  </action>
  <acceptance_criteria>
    <criterion>`src/bagels/__main__.py` does not contain `from bagels.cli.git import`</criterion>
    <criterion>`src/bagels/__main__.py` does not contain `name="git"`, `name="export"`, or `name="import"` in `cli.add_command` calls</criterion>
    <criterion>`src/bagels/cli/__init__.py` has no exported symbol for removed sync commands</criterion>
  </acceptance_criteria>
  <verify>
    rg -n "from bagels\.cli\.(git|export|import)|name=\"(git|export|import)\"" src/bagels/__main__.py src/bagels/cli/__init__.py
  </verify>
  <done>Top-level CLI no longer wires sync commands; only supported commands are visible.</done>
</task>

<task type="code">
  <name>Remove startup YAML sync execution path</name>
  <files>src/bagels/app.py</files>
  <read_first>
    <file>src/bagels/app.py</file>
    <file>src/bagels/importer/importer.py</file>
    <file>src/bagels/git/operations.py</file>
  </read_first>
  <action>
    Remove `self.run_startup_import()` call from `App.on_mount()` and remove the `run_startup_import` worker implementation.
    Preserve all non-sync app initialization behavior (theme, jumper, layout/reactive behavior).
    Do not add replacement background sync.
  </action>
  <acceptance_criteria>
    <criterion>`src/bagels/app.py` does not contain `run_startup_import` method definition</criterion>
    <criterion>`src/bagels/app.py` does not call `self.run_startup_import()`</criterion>
    <criterion>`src/bagels/app.py` imports no sync-only symbols (`pull_from_remote`, `run_full_import`) for startup path</criterion>
  </acceptance_criteria>
  <verify>
    rg -n "run_startup_import|pull_from_remote|run_full_import" src/bagels/app.py
  </verify>
  <done>Application startup contains no Git/YAML sync side effects.</done>
</task>

<task type="code">
  <name>Add one-time deprecation warning for legacy sync config</name>
  <files>src/bagels/config.py</files>
  <read_first>
    <file>src/bagels/config.py</file>
    <file>.planning/phases/05-requirement-reduction-remove-buggy-git-and-yaml-loader/05-CONTEXT.md</file>
  </read_first>
  <action>
    Add a one-time warning in `load_config()` that triggers when legacy sync-related `git.*` settings indicate sync intent (`enabled`, `auto_commit`, `auto_push`, `auto_pull`, custom `remote`, custom `branch`, or non-null `commit_message_format`).
    Persist a state key in config (for example `state.sync_deprecation_warned: true`) after first warning so it is not repeated every startup.
    Keep parsing of `git.*` fields intact for backward compatibility.
  </action>
  <acceptance_criteria>
    <criterion>`src/bagels/config.py` contains exactly one persisted warning guard key for deprecation behavior</criterion>
    <criterion>warning text references both `Git` and `YAML sync` removal</criterion>
    <criterion>warning path does not prevent app startup and does not raise exceptions</criterion>
  </acceptance_criteria>
  <verify>
    rg -n "sync_deprecation_warned|YAML sync|Git sync|deprecat" src/bagels/config.py
  </verify>
  <done>Legacy sync config is accepted but users receive a one-time migration warning.</done>
</task>

</tasks>

<verification>
Run CLI smoke checks after implementation:
`uv run python -m bagels --help`
`uv run python -m bagels git --help` (should fail as unknown command)
</verification>
