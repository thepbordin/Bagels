---
phase: 05-requirement-reduction-remove-buggy-git-and-yaml-loader
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bagels/managers/accounts.py
  - src/bagels/managers/categories.py
  - src/bagels/managers/persons.py
  - src/bagels/managers/record_templates.py
  - src/bagels/managers/records.py
  - src/bagels/cli/git.py
  - src/bagels/cli/export.py
  - src/bagels/cli/import.py
  - src/bagels/cli/init.py
  - src/bagels/git/operations.py
  - src/bagels/git/repository.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Manager CRUD operations no longer trigger background YAML export or git commit side effects"
    - "Sync-only CLI modules and git operations are removed from active runtime path"
    - "`bagels init` no longer performs Git repository initialization"
  artifacts:
    - path: "src/bagels/managers/records.py"
      provides: "Record CRUD without `_trigger_export_and_commit` background thread"
      min_lines: 120
    - path: "src/bagels/managers/accounts.py"
      provides: "Account CRUD without `_trigger_entity_export` side effects"
      min_lines: 100
    - path: "src/bagels/cli/init.py"
      provides: "SQLite-only init behavior without gitpython usage"
      min_lines: 40
  key_links:
    - from: "src/bagels/managers/accounts.py"
      to: "src/bagels/export/exporter.py"
      via: "export hook path removed from create/update/delete"
    - from: "src/bagels/managers/records.py"
      to: "src/bagels/git/operations.py::auto_commit_yaml"
      via: "record commit path removed"
---

<objective>
Eliminate runtime sync side effects from manager CRUD and remove remaining Git/YAML sync implementation entrypoints that are no longer valid under SQLite-only operation.
</objective>

<execution_context>
@/Users/thepbordin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/thepbordin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-requirement-reduction-remove-buggy-git-and-yaml-loader/05-CONTEXT.md
@src/bagels/managers/accounts.py
@src/bagels/managers/records.py
</context>

<tasks>

<task type="code">
  <name>Remove manager-level auto export and auto commit hooks</name>
  <files>src/bagels/managers/accounts.py, src/bagels/managers/categories.py, src/bagels/managers/persons.py, src/bagels/managers/record_templates.py, src/bagels/managers/records.py</files>
  <read_first>
    <file>src/bagels/managers/accounts.py</file>
    <file>src/bagels/managers/categories.py</file>
    <file>src/bagels/managers/persons.py</file>
    <file>src/bagels/managers/record_templates.py</file>
    <file>src/bagels/managers/records.py</file>
  </read_first>
  <action>
    Remove `_trigger_entity_export` and `_trigger_export_and_commit` helper functions and remove all `threading.Thread(..., daemon=True)` calls that invoke those helpers.
    Keep existing CRUD persistence behavior (session add/commit/refresh/return values) unchanged.
    Remove now-unused imports (`threading`, `logging`, sync module imports) from each manager module.
  </action>
  <acceptance_criteria>
    <criterion>none of the listed manager files contains `_trigger_entity_export`</criterion>
    <criterion>`src/bagels/managers/records.py` does not contain `_trigger_export_and_commit`</criterion>
    <criterion>none of the listed manager files contains `threading.Thread(`</criterion>
    <criterion>manager CRUD tests still pass for create/update/delete behavior</criterion>
  </acceptance_criteria>
  <verify>
    rg -n "_trigger_entity_export|_trigger_export_and_commit|threading\.Thread\(" src/bagels/managers/accounts.py src/bagels/managers/categories.py src/bagels/managers/persons.py src/bagels/managers/record_templates.py src/bagels/managers/records.py
  </verify>
  <done>CRUD writes only to SQLite and no longer spawns sync threads.</done>
</task>

<task type="code">
  <name>Retire sync-only CLI and git operation modules</name>
  <files>src/bagels/cli/git.py, src/bagels/cli/export.py, src/bagels/cli/import.py, src/bagels/git/operations.py, src/bagels/git/repository.py</files>
  <read_first>
    <file>src/bagels/cli/git.py</file>
    <file>src/bagels/cli/export.py</file>
    <file>src/bagels/cli/import.py</file>
    <file>src/bagels/git/operations.py</file>
    <file>src/bagels/git/repository.py</file>
    <file>src/bagels/__main__.py</file>
  </read_first>
  <action>
    Remove sync-only CLI command modules (`git.py`, `export.py`, `import.py`) and remove git operations implementation module references from runtime code.
    If direct deletion causes import breakage in package exports, replace module contents with lightweight compatibility stubs that raise `RuntimeError("Git/YAML sync removed in Phase 5")` and ensure those stubs are not imported during normal startup.
    Remove any now-dead git helper code paths that are unreachable after command deregistration.
  </action>
  <acceptance_criteria>
    <criterion>`src/bagels/__main__.py` and manager/app modules import no symbol from `bagels.git.operations`</criterion>
    <criterion>sync-only CLI modules are either deleted or converted to explicit removal stubs</criterion>
    <criterion>normal `uv run python -m bagels --help` startup does not import removed sync modules</criterion>
  </acceptance_criteria>
  <verify>
    rg -n "bagels\.git\.operations|from bagels\.cli\.(git|export|import)" src/bagels
  </verify>
  <done>Runtime path no longer depends on sync-only CLI/git modules.</done>
</task>

<task type="code">
  <name>Convert `bagels init` to SQLite-only bootstrap behavior</name>
  <files>src/bagels/cli/init.py</files>
  <read_first>
    <file>src/bagels/cli/init.py</file>
    <file>src/bagels/locations.py</file>
    <file>src/bagels/models/database/app.py</file>
  </read_first>
  <action>
    Rewrite `init_command` so it initializes local app directories/database state only and removes Git repository creation, `.gitignore` creation, and gitpython commit logic.
    Keep command name `bagels init` and update user messages to SQLite-only guidance.
    Remove `--force` behavior tied to Git reinitialization unless replaced with explicit non-git directory reset semantics.
  </action>
  <acceptance_criteria>
    <criterion>`src/bagels/cli/init.py` does not import `bagels.git.repository` or `git.Repo`</criterion>
    <criterion>`src/bagels/cli/init.py` output/help text contains no git remote/push instructions</criterion>
    <criterion>`uv run python -m bagels init --help` succeeds and describes non-git initialization</criterion>
  </acceptance_criteria>
  <verify>
    rg -n "git|Repo|remote|push" src/bagels/cli/init.py
  </verify>
  <done>Initialization command reflects SQLite-only product direction.</done>
</task>

</tasks>

<verification>
Run focused behavior checks:
`uv run pytest tests/managers -q`
`uv run python -m bagels init --help`
</verification>
