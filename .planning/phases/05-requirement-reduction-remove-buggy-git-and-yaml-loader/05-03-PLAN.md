---
phase: 05-requirement-reduction-remove-buggy-git-and-yaml-loader
plan: "03"
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - tests/automation/test_hooks.py
  - tests/automation/test_startup.py
  - tests/git/test_operations.py
  - tests/cli/test_main.py
  - tests/integration/test_auto_export_triggers.py
  - tests/integration/test_git_conflict.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Requirement docs explicitly mark legacy Git/YAML sync requirements as superseded by Phase 5 reduction"
    - "Test suite no longer expects removed sync command paths or startup YAML import behavior"
    - "Full `uv run pytest` passes after SQLite-only migration"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement baseline updated with superseded sync requirements and reduction replacements"
      min_lines: 120
    - path: "tests/cli/test_main.py"
      provides: "CLI coverage for removed commands returning unknown-command behavior"
      min_lines: 40
    - path: "tests/automation/test_startup.py"
      provides: "Updated startup expectations without auto-pull/import behavior"
      min_lines: 40
  key_links:
    - from: "tests/cli/test_main.py"
      to: "src/bagels/__main__.py"
      via: "verifies removed sync commands are unavailable"
    - from: "tests/automation/test_startup.py"
      to: "src/bagels/app.py"
      via: "verifies startup path no longer performs YAML sync"
---

<objective>
Rebaseline requirements and verification to the SQLite-only architecture, replace obsolete sync-focused tests with reduction-focused checks, and close the phase with a full green pytest run.
</objective>

<execution_context>
@/Users/thepbordin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/thepbordin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-requirement-reduction-remove-buggy-git-and-yaml-loader/05-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="docs">
  <name>Update requirements and roadmap for reduction baseline</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/phases/05-requirement-reduction-remove-buggy-git-and-yaml-loader/05-CONTEXT.md</file>
  </read_first>
  <action>
    In `REQUIREMENTS.md`, mark sync-centric items as superseded for v1 runtime (`DATA-07`, `DATA-08`, `GIT-02` through `GIT-08`, and sync behavior tied to removed commands), and add replacement reduction requirements such as:
    `REDUCE-01 SQLite-only runtime`,
    `REDUCE-02 removed sync commands`,
    `REDUCE-03 legacy config warning once`,
    `REDUCE-04 full test gate`.
    In `ROADMAP.md` Phase 5 section, replace `[To be planned]` goal text with a concrete reduction goal and enumerate plan IDs `05-01`, `05-02`, `05-03`.
  </action>
  <acceptance_criteria>
    <criterion>`.planning/REQUIREMENTS.md` contains `REDUCE-01`, `REDUCE-02`, `REDUCE-03`, and `REDUCE-04` entries</criterion>
    <criterion>`.planning/REQUIREMENTS.md` marks superseded sync requirements explicitly with the word `superseded`</criterion>
    <criterion>`.planning/ROADMAP.md` Phase 5 goal is concrete and no longer `[To be planned]`</criterion>
  </acceptance_criteria>
  <verify>
    rg -n "REDUCE-0[1-4]|superseded|Phase 5|05-01-PLAN|05-02-PLAN|05-03-PLAN" .planning/REQUIREMENTS.md .planning/ROADMAP.md
  </verify>
  <done>Planning docs match the new SQLite-only product direction.</done>
</task>

<task type="code">
  <name>Replace obsolete sync tests with reduction-focused assertions</name>
  <files>tests/automation/test_hooks.py, tests/automation/test_startup.py, tests/git/test_operations.py, tests/cli/test_main.py, tests/integration/test_auto_export_triggers.py, tests/integration/test_git_conflict.py</files>
  <read_first>
    <file>tests/automation/test_hooks.py</file>
    <file>tests/automation/test_startup.py</file>
    <file>tests/git/test_operations.py</file>
    <file>src/bagels/__main__.py</file>
    <file>src/bagels/app.py</file>
  </read_first>
  <action>
    Remove or rewrite tests that assert removed sync behavior (auto-export hooks, startup import/pull, git operation helpers, importer conflict gate for runtime startup).
    Add/adjust CLI tests in `tests/cli/test_main.py` for removed commands: invoking `bagels git`, `bagels export`, `bagels import` should return non-zero with unknown-command semantics.
    Add/adjust config/startup tests to verify one-time deprecation warning behavior and that startup does not invoke YAML sync.
    Keep YAML output formatter tests for query commands intact.
  </action>
  <acceptance_criteria>
    <criterion>no active test asserts automatic YAML export on manager CRUD</criterion>
    <criterion>`tests/cli/test_main.py` contains explicit unknown-command checks for `git`, `export`, and `import`</criterion>
    <criterion>startup test suite has no expectation that `run_full_import` or `pull_from_remote` is called on mount</criterion>
  </acceptance_criteria>
  <verify>
    rg -n "run_full_import|pull_from_remote|auto_commit_yaml|bagels git|bagels export|bagels import" tests
  </verify>
  <done>Test intent matches reduced runtime behavior.</done>
</task>

<task type="code">
  <name>Run full pytest gate and resolve remaining failures</name>
  <files>tests/, src/</files>
  <read_first>
    <file>pyproject.toml</file>
    <file>tests/conftest.py</file>
    <file>src/bagels/__main__.py</file>
  </read_first>
  <action>
    Run `uv run pytest` for the full repository.
    For each failure, either:
    1) update code to preserve intended SQLite-only behavior, or
    2) update tests when the previous assertion covered removed sync features.
    Do not ship phase output until full suite is green.
  </action>
  <acceptance_criteria>
    <criterion>`uv run pytest` exits with status code 0</criterion>
    <criterion>no skipped tests are introduced solely to hide unresolved failures</criterion>
    <criterion>phase summary captures final test totals and changed test scope</criterion>
  </acceptance_criteria>
  <verify>
    uv run pytest
  </verify>
  <done>Repository test suite is fully passing with SQLite-only semantics.</done>
</task>

</tasks>

<verification>
Primary gate: `uv run pytest`
Secondary: `node /Users/thepbordin/.codex/get-shit-done/bin/gsd-tools.cjs verify phase-completeness 05`
</verification>
