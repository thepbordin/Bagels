---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/conftest.py, tests/export/test_accounts.py, tests/export/test_categories.py, tests/export/test_persons.py, tests/export/test_templates.py]
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, FMT-01, FMT-03, FMT-05]
must_haves:
  truths:
    - Tests can verify account export to YAML format
    - Tests can verify category export to YAML format
    - Tests can verify person export to YAML format
    - Tests can verify template export to YAML format
  artifacts:
    - path: "tests/conftest.py"
      provides: "Shared test fixtures for database, temp directories"
      min_lines: 50
    - path: "tests/export/test_accounts.py"
      provides: "Account export validation tests"
      min_lines: 30
    - path: "tests/export/test_categories.py"
      provides: "Category export validation tests"
      min_lines: 30
    - path: "tests/export/test_persons.py"
      provides: "Person export validation tests"
      min_lines: 30
    - path: "tests/export/test_templates.py"
      provides: "Template export validation tests"
      min_lines: 30
  key_links:
    - from: "tests/export/test_*.py"
      to: "conftest.py"
      via: "pytest fixtures"
      pattern: "@pytest.fixture"
---

<objective>
Create test infrastructure and entity export tests (accounts, categories, persons, templates) using TDD approach. These tests define expected behavior for exporting core entities to YAML format.

Purpose: Test-Driven Development ensures export implementations meet requirements from the start. Tests serve as living documentation and prevent regressions for core entity types.
Output: Test fixtures and 4 entity test files (accounts, categories, persons, templates) with failing tests (RED phase of TDD).
</objective>

<execution_context>
@/Users/thepbordin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thepbordin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@tests/snapshot.py
@src/bagels/models/account.py
@src/bagels/models/record.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared test fixtures (Wave 0)</name>
  <files>tests/conftest.py</files>
  <action>
Create pytest fixtures for export/import testing:

1. **in_memory_db fixture**: SQLAlchemy session with in-memory SQLite database
   - Create all tables using Base.metadata.create_all()
   - Return session object
   - Clean up after each test (session.close())

2. **temp_directory fixture**: Temporary directory for YAML file operations
   - Use tmp_path fixture or tempfile.mkdtemp()
   - Return Path object
   - Clean up after tests

3. **sample_data fixtures**: Pre-populated database fixtures for each entity type
   - sample_account: Account(name="Savings", beginningBalance=1000.0)
   - sample_category: Category(name="Groceries", parent=None)
   - sample_person: Person(name="John Doe")
   - sample_template: RecordTemplate with all fields
   - sample_records: List of 5 Records across different dates

4. **yaml_file fixture**: Helper to create/validate YAML files
   - Function that writes YAML data to temp directory
   - Returns file path for validation

Use existing test patterns from tests/snapshot.py and tests/managers/ as reference.
  </action>
  <verify>
pytest tests/conftest.py --collect-only  # Verify fixtures are discoverable
pytest tests/conftest.py -v  # Should show no tests (only fixtures)
  </verify>
  <done>
- conftest.py created with 4+ fixtures
- Fixtures are importable by test files
- in_memory_db creates clean database per test
- temp_directory provides isolated file system
  </done>
</task>

<task type="auto">
  <name>Task 2: Create account export tests (TDD - RED)</name>
  <files>tests/export/test_accounts.py</files>
  <action>
Create tests for account export to YAML with dict-based structure:

**Test 1: Export single account**
- Given: Account with name="Savings", description="Emergency fund", beginningBalance=1000.0
- When: export_account_to_yaml(account, temp_dir)
- Then: File exists at temp_dir/accounts.yaml
- And: YAML contains dict keyed by account slug
- And: All fields exported (name, description, beginningBalance, createdAt, updatedAt)
- And: Amounts exported as floats with proper precision

**Test 2: Export multiple accounts**
- Given: 3 accounts in database
- When: export_all_accounts(session, temp_dir)
- Then: YAML contains all 3 accounts
- And: Account slugs are keys in dict
- And: No integer IDs exposed (only slugs)

**Test 3: Export includes metadata**
- Given: Account with createdAt and updatedAt timestamps
- When: Exported to YAML
- Then: Timestamps in ISO 8601 format (YYYY-MM-DDTHH:MM:SS)
- And: Timestamps match database values

**Test 4: Export handles null fields**
- Given: Account with description=None, repaymentDate=None
- When: Exported to YAML
- Then: Null fields represented as null in YAML
- And: No empty strings or "None" strings

Use pytest and assert statements. Do NOT implement export functions yet - this is the RED phase.
  </action>
  <verify>
pytest tests/export/test_accounts.py -x -v  # Should fail (functions don't exist)
  </verify>
  <done>
- test_accounts.py created with 4+ test cases
- Tests fail with ModuleNotFoundError or AttributeError (expected - RED phase)
- Tests cover all requirements from DATA-01 and FMT-01
  </done>
</task>

<task type="auto">
  <name>Task 3: Create category export tests (TDD - RED)</name>
  <files>tests/export/test_categories.py</files>
  <action>
Create tests for category export to YAML with recursive structure:

**Test 1: Export single category without parent**
- Given: Category(name="Groceries", parent=None)
- When: export_category_to_yaml(category, temp_dir)
- Then: YAML contains category with slug key
- And: parent field is null

**Test 2: Export category with parent-child relationship**
- Given: Parent category "Food" with child "Groceries"
- When: export_categories_to_yaml(session, temp_dir)
- Then: YAML contains both categories
- And: Child category has parentSlug reference
- And: Structure preserves hierarchy

**Test 3: Export nested category tree**
- Given: 3-level hierarchy: Expenses → Food → Groceries
- When: Exported to YAML
- Then: All 3 levels exported
- And: parentSlug references use slugs (not IDs)
- And: Structure allows reconstruction of tree

**Test 4: Export handles multiple root categories**
- Given: 3 root categories with different children
- When: Exported to YAML
- Then: All root categories present
- And: Children correctly reference parents
  </action>
  <verify>
pytest tests/export/test_categories.py -x -v  # Should fail (RED phase)
  </verify>
  <done>
- test_categories.py created with 4+ test cases
- Tests fail (expected - RED phase)
- Tests cover DATA-02 and recursive structure requirements
  </done>
</task>

<task type="auto">
  <name>Task 4: Create person export tests (TDD - RED)</name>
  <files>tests/export/test_persons.py</files>
  <action>
Create tests for person export to YAML:

**Test 1: Export single person**
- Given: Person(name="John Doe")
- When: export_person_to_yaml(person, temp_dir)
- Then: YAML contains person with slug key
- And: Name field exported

**Test 2: Export multiple persons**
- Given: 5 persons in database
- When: export_all_persons(session, temp_dir)
- Then: YAML contains all 5 persons
- And: Dict keyed by person slugs

**Test 3: Export person with metadata**
- Given: Person with createdAt and updatedAt
- When: Exported to YAML
- Then: Timestamps in ISO format

Keep tests simple - Person model has minimal fields per context from existing codebase.
  </action>
  <verify>
pytest tests/export/test_persons.py -x -v  # Should fail (RED phase)
  </verify>
  <done>
- test_persons.py created with 3+ test cases
- Tests fail (expected - RED phase)
- Tests cover DATA-03 requirements
  </done>
</task>

<task type="auto">
  <name>Task 5: Create template export tests (TDD - RED)</name>
  <files>tests/export/test_templates.py</files>
  <action>
Create tests for record template export to YAML:

**Test 1: Export single template**
- Given: RecordTemplate with label, amount, accountId, categoryId
- When: export_template_to_yaml(template, temp_dir)
- Then: YAML contains template with slug key
- And: All fields exported
- And: Foreign keys reference slugs (accountSlug, categorySlug)

**Test 2: Export multiple templates**
- Given: 3 templates in database
- When: export_all_templates(session, temp_dir)
- Then: YAML contains all 3 templates
- And: Order preserved (templates have order field)

**Test 3: Export template with all relationships**
- Given: Template with accountId, categoryId, personId
- When: Exported to YAML
- Then: All relationships use slug references
- And: No integer IDs in YAML

Verify RecordTemplate model structure first from src/bagels/models/record_template.py.
  </action>
  <verify>
pytest tests/export/test_templates.py -x -v  # Should fail (RED phase)
  </verify>
  <done>
- test_templates.py created with 3+ test cases
- Tests fail (expected - RED phase)
- Tests cover DATA-04 requirements
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. **Test Discovery**: Verify all tests are discoverable by pytest
   ```bash
   pytest tests/export/ --collect-only
   ```
   Expected: 14+ tests collected across 4 test files

2. **Red Phase Confirmation**: Verify all tests fail (functions don't exist)
   ```bash
   pytest tests/export/ -x -v
   ```
   Expected: All tests fail with ModuleNotFoundError or AttributeError

3. **Fixture Validation**: Verify fixtures work correctly
   ```bash
   pytest tests/conftest.py --fixtures
   ```
   Expected: Shows in_memory_db, temp_directory, sample_* fixtures

4. **Coverage Check**: Verify test files cover all entity types
   ```bash
   pytest tests/export/test_accounts.py tests/export/test_categories.py tests/export/test_persons.py tests/export/test_templates.py --collect-only -q | grep "test_"
   ```
   Expected: Tests for accounts, categories, persons, templates
</verification>

<success_criteria>
1. Test infrastructure complete with shared fixtures in conftest.py
2. Four entity test files created (accounts, categories, persons, templates)
3. All 14+ tests failing as expected (RED phase of TDD)
4. Tests serve as executable documentation for entity export behavior
5. Tests are ready for implementation phase (GREEN phase in Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- Number of tests created
- Test coverage by entity type
- List of fixtures created
- Confirmation that all tests fail (RED phase complete)
</output>
