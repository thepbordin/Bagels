---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [tests/import/test_import.py, tests/import/test_validator.py]
autonomous: true
requirements: [DATA-06, FMT-01, FMT-02, FMT-03, FMT-04, FMT-05]
must_haves:
  truths:
    - Import validation prevents corrupt data from entering database
    - Import merge-by-ID strategy preserves YAML changes across sync cycles
    - Import referential integrity checks prevent orphaned records
    - Import backup creation enables recovery from failed imports
  artifacts:
    - path: "tests/import/test_import.py"
      provides: "Import functionality tests"
      min_lines: 80
    - path: "tests/import/test_validator.py"
      provides: "YAML validation logic tests"
      min_lines: 60
  key_links:
    - from: "tests/import/test_import.py"
      to: "tests/conftest.py"
      via: "pytest fixtures"
      pattern: "@pytest.fixture"
    - from: "tests/import/test_validator.py"
      to: "tests/conftest.py"
      via: "pytest fixtures"
      pattern: "@pytest.fixture"
---

<objective>
Create comprehensive test suite for YAML import functionality using TDD approach. These tests define the expected behavior for importing YAML files back into SQLite with validation, merge-by-ID strategy, and referential integrity checks.

Purpose: Test-Driven Development ensures import implementations handle edge cases correctly (merge conflicts, broken references, data validation). Tests serve as living documentation for import behavior.
Output: Complete test suite covering all import scenarios (validation, merge-by-ID, referential integrity, backup creation).
</objective>

<execution_context>
@/Users/thepbordin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thepbordin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@src/bagels/models/account.py
@src/bagels/models/record.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create YAML validation tests (TDD - RED)</name>
  <files>tests/import/test_validator.py</files>
  <action>
Create tests for YAML validation logic before import:

**Test 1: Validate accounts YAML structure**
- Given: Valid accounts YAML with dict structure
- When: validate_accounts_yaml(yaml_data)
- Then: Returns (True, [])
- And: No validation errors

**Test 2: Detect missing required fields**
- Given: Accounts YAML missing 'name' field
- When: validate_accounts_yaml(yaml_data)
- Then: Returns (False, [error_list])
- And: Error specifies "Missing required field 'name'"

**Test 3: Detect broken foreign key references**
- Given: Record YAML with accountSlug pointing to non-existent account
- When: validate_records_yaml(yaml_data, session)
- Then: Returns (False, [error_list])
- And: Error specifies "Referenced account 'acc_missing' does not exist"

**Test 4: Validate all entities then prompt**
- Given: YAML with multiple validation errors
- When: validate_yaml(yaml_data, entity_type, session)
- Then: Returns (False, [all_errors])
- And: All errors listed together (not fail-fast)

**Test 5: Validate timestamp format**
- Given: YAML with invalid timestamp format
- When: validate_accounts_yaml(yaml_data)
- Then: Returns (False, [error_list])
- And: Error specifies "Invalid timestamp format"

**Test 6: Validate monetary value format**
- Given: YAML with amount as string instead of float
- When: validate_records_yaml(yaml_data)
- Then: Returns (False, [error_list])
- And: Error specifies "Amount must be a number"

**Test 7: Validate slug format**
- Given: Record YAML with invalid slug format (not r_YYYY-MM-DD_###)
- When: validate_records_yaml(yaml_data)
- Then: Returns (False, [error_list])
- And: Error specifies "Invalid slug format"

Validation is critical for "fail fast on broken references" requirement from CONTEXT.md.
  </action>
  <verify>
pytest tests/import/test_validator.py -x -v  # Should fail (RED phase)
  </verify>
  <done>
- test_validator.py created with 7+ test cases
- Tests fail (expected - RED phase)
- Tests cover validation requirements from CONTEXT.md
- Error reporting format specified
  </done>
</task>

<task type="auto">
  <name>Task 2: Create import tests with merge-by-ID strategy (TDD - RED)</name>
  <files>tests/import/test_import.py</files>
  <action>
Create tests for YAML import with merge-by-ID strategy:

**Test 1: Import new accounts**
- Given: YAML with 2 new accounts (not in database)
- When: import_accounts_from_yaml(yaml_data, session)
- Then: 2 accounts created in database
- And: Slugs match YAML keys
- And: All fields imported correctly

**Test 2: Import updates existing records (merge-by-ID)**
- Given: Database has record with slug r_2026-03-14_001 (amount=50.0)
- And: YAML has same slug with amount=75.0
- When: import_records_from_yaml(yaml_data, session)
- Then: Record updated with amount=75.0
- And: YAML is authoritative (YAML data overwrites SQLite)

**Test 3: Import adds new records**
- Given: Database has 3 records
- And: YAML has 5 records (2 existing, 3 new)
- When: import_records_from_yaml(yaml_data, session)
- Then: Database has 5 records total
- And: 2 existing updated, 3 new created

**Test 4: Import handles monthly record files**
- Given: YAML file for 2026-03.yaml with 10 records
- When: import_records_from_yaml(yaml_data, session)
- Then: All 10 records imported
- And: Dates match March 2026
- And: Slugs preserved from YAML

**Test 5: Import preserves relationships via slug references**
- Given: YAML with records referencing accountSlug="acc_savings"
- And: acc_savings exists in database
- When: import_records_from_yaml(yaml_data, session)
- Then: Records imported successfully
- And: accountId foreign key set correctly

**Test 6: Import fails on broken references**
- Given: YAML with records referencing accountSlug="acc_missing"
- And: acc_missing does NOT exist in database
- When: import_records_from_yaml(yaml_data, session)
- Then: Import raises ValidationError
- And: No records imported (fail-fast)
- And: Error message specifies missing account

**Test 7: Import validates before importing**
- Given: YAML with multiple validation errors
- When: import_records_from_yaml(yaml_data, session)
- Then: Validation runs first
- And: All errors reported together
- And: Import aborted (no partial import)

**Test 8: Import creates backup before importing**
- Given: Database with existing data
- When: import_accounts_from_yaml(yaml_data, session)
- Then: Backup created at backups/backup_YYYY-MM-DD_HHMMSS.db
- And: Backup file exists and is valid SQLite database
- And: Backup contains pre-import data

**Test 9: Import handles category parent-child relationships**
- Given: YAML with parent and child categories
- When: import_categories_from_yaml(yaml_data, session)
- Then: Parent and child imported
- And: Parent-child relationship preserved via parentSlug

**Test 10: Import handles templates with order**
- Given: YAML with 3 templates in specific order
- When: import_templates_from_yaml(yaml_data, session)
- Then: All 3 templates imported
- And: Order field preserved from YAML

**Test 11: Import is idempotent**
- Given: YAML with existing data
- When: Import twice with same YAML
- Then: Second import produces same state
- And: No duplicate records created

**Test 12: Import handles large datasets efficiently**
- Given: YAML with 1000 records
- When: import_records_from_yaml(yaml_data, session)
- Then: Import completes in reasonable time (<10 seconds)
- And: Bulk operations used (not row-by-row)

Merge-by-ID strategy is critical for "YAML is authoritative" requirement from CONTEXT.md.
  </action>
  <verify>
pytest tests/import/test_import.py -x -v  # Should fail (RED phase)
  </verify>
  <done>
- test_import.py created with 12+ test cases
- Tests fail (expected - RED phase)
- Tests cover merge-by-ID, validation, backup, referential integrity
- Tests cover all entity types
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. **Test Discovery**: Verify all import tests are discoverable
   ```bash
   pytest tests/import/ --collect-only
   ```
   Expected: 19+ tests collected across 2 test files

2. **Red Phase Confirmation**: Verify all tests fail (functions don't exist)
   ```bash
   pytest tests/import/ -x -v
   ```
   Expected: All tests fail with ModuleNotFoundError or AttributeError

3. **Test Coverage**: Verify all import scenarios covered
   ```bash
   pytest tests/import/ --collect-only -q | grep "test_"
   ```
   Expected: Tests for validation, merge-by-ID, referential integrity, backup creation, all entity types

4. **Cross-reference with Requirements**: Verify DATA-06 and all FMT requirements covered
   ```bash
   grep -r "DATA-06\|FMT-" tests/import/
   ```
   Expected: All requirements referenced in test docstrings or comments
</verification>

<success_criteria>
1. Test infrastructure complete for import functionality
2. Two test files created (validation, import operations)
3. All 19+ tests failing as expected (RED phase complete)
4. Tests cover validation, merge-by-ID strategy, referential integrity, backup creation
5. Tests serve as executable documentation for import behavior
6. Tests ready for implementation phase (GREEN phase in Plan 04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` with:
- Number of tests created
- Test coverage by scenario (validation, merge, integrity, backup)
- Confirmation that all tests fail (RED phase complete)
- List of edge cases covered
</output>
