---
phase: 02-cli-query-layer
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/cli/conftest.py
  - tests/cli/test_records.py
  - tests/cli/test_summary.py
  - tests/cli/test_accounts.py
  - tests/cli/test_categories.py
  - tests/cli/test_spending.py
  - tests/cli/test_trends.py
  - tests/cli/test_llm.py
  - tests/cli/test_schema.py
  - tests/cli/test_output_formatters.py
  - tests/conftest.py
autonomous: true
requirements: []
must_haves:
  truths:
    - All CLI test files exist with placeholder tests
    - Shared fixtures available in tests/cli/conftest.py
    - Test files import correctly without errors
    - pytest can discover all CLI test files
  artifacts:
    - path: tests/cli/conftest.py
      provides: Shared CLI test fixtures
      exports: ["cli_runner", "sample_db_with_records", "sample_accounts", "sample_categories", "sample_records"]
      min_lines: 80
    - path: tests/cli/test_records.py
      provides: Placeholder tests for CLI-01, CLI-02, CLI-03, CLI-10
      exports: ["test_records_list_default", "test_records_list_month", "test_records_filter_category"]
      min_lines: 50
    - path: tests/cli/test_summary.py
      provides: Placeholder tests for CLI-03
      exports: ["test_summary_default_month", "test_summary_specific_month"]
      min_lines: 40
    - path: tests/cli/test_accounts.py
      provides: Placeholder tests for CLI-04
      exports: ["test_accounts_list_default", "test_accounts_yaml_format"]
      min_lines: 40
    - path: tests/cli/test_categories.py
      provides: Placeholder tests for CLI-05
      exports: ["test_categories_tree", "test_categories_tree_json"]
      min_lines: 40
    - path: tests/cli/test_spending.py
      provides: Placeholder tests for CLI-06, CLI-07
      exports: ["test_spending_by_category", "test_spending_by_day"]
      min_lines: 50
    - path: tests/cli/test_trends.py
      provides: Placeholder tests for CLI-08
      exports: ["test_trends_default_months", "test_trends_category_filter"]
      min_lines: 40
    - path: tests/cli/test_llm.py
      provides: Placeholder tests for LLM-01, LLM-02, LLM-03, LLM-04, LLM-05
      exports: ["test_llm_context_month", "test_context_completeness", "test_context_budget_status"]
      min_lines: 60
    - path: tests/cli/test_schema.py
      provides: Placeholder tests for LLM-02, LLM-03
      exports: ["test_schema_full", "test_schema_model"]
      min_lines: 40
    - path: tests/cli/test_output_formatters.py
      provides: Placeholder tests for CLI-09
      exports: ["test_format_records_table", "test_format_records_json", "test_json_datetime_serialization"]
      min_lines: 50
  key_links:
    - from: All CLI test files
      to: tests/cli/conftest.py
      via: pytest fixture imports
      pattern: "from tests.cli.conftest import"
    - from: pytest
      to: tests/cli/test_*.py
      via: test discovery
      pattern: "pytest tests/cli/ -v"
---

<objective>
Create CLI test infrastructure with shared fixtures and placeholder test files for all Phase 2 requirements.

Purpose: Establish test infrastructure foundation before Wave 1 execution. This plan creates all test file stubs and shared fixtures, enabling Wave 1-3 plans to reference test files in their verification commands without Nyquist compliance failures.

Output: Complete CLI test infrastructure with shared fixtures and placeholder tests for all requirements.
</objective>

<execution_context>
@/Users/thepbordin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thepbordin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cli-query-layer/02-CONTEXT.md
@.planning/phases/02-cli-query-layer/02-RESEARCH.md
@.planning/phases/02-cli-query-layer/02-VALIDATION.md

# Existing test patterns
@tests/conftest.py
</context>

<interfaces>
<!-- From existing test infrastructure -->
From tests/conftest.py:
```python
# Existing fixtures from Phase 1
@pytest.fixture
def in_memory_db():
    """Create in-memory SQLite database for testing."""

@pytest.fixture
def session(in_memory_db):
    """Create a database session."""
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create shared CLI test fixtures</name>
  <files>tests/cli/conftest.py</files>
  <action>
Create shared test fixtures for CLI testing:

1. **cli_runner fixture**:
   ```python
   from click.testing import CliRunner

   @pytest.fixture
   def cli_runner():
       """Provide Click CliRunner for CLI command testing."""
       return CliRunner()
   ```

2. **sample_db_with_records fixture**:
   - Create in-memory database with comprehensive test data
   - Reuse in_memory_db pattern from tests/conftest.py
   - Create 3-5 sample accounts (Savings, Checking, Credit Card)
   - Create 5-10 sample categories with hierarchy (Food > Groceries/Restaurants, Transport > Gas/Public Transit)
   - Create 20-30 sample records spanning 2-3 months (2026-01, 2026-02, 2026-03)
   - Include various amounts, dates, categories for realistic query testing
   - Include both income and expense records
   - Include records with splits (split across multiple categories)

3. **Sample data fixtures** (quick access):
   ```python
   @pytest.fixture
   def sample_accounts(sample_db_with_records):
       """Return list of Account objects from sample DB."""

   @pytest.fixture
   def sample_categories(sample_db_with_records):
       """Return list of Category objects from sample DB."""

   @pytest.fixture
   def sample_records(sample_db_with_records):
       """Return list of Record objects from sample DB."""
   ```

4. **Session fixture for CLI tests**:
   ```python
   @pytest.fixture
   def cli_session(sample_db_with_records):
       """Provide session for CLI test queries."""
       # Return session from sample_db_with_records
   ```

Follow pytest fixture patterns. Ensure fixtures are importable by all CLI test files.
  </action>
  <verify>
pytest tests/cli/conftest.py -v (no failures on fixture import)
python -c "from tests.cli.conftest import cli_runner, sample_db_with_records; print('Fixtures importable')"
  </verify>
  <done>CLI test fixtures available for all query command tests</done>
</task>

<task type="auto">
  <name>Task 2: Create records test stubs</name>
  <files>tests/cli/test_records.py</files>
  <action>
Create placeholder tests for records commands (CLI-01, CLI-02, CLI-03, CLI-10):

1. **Import fixtures and CLI**:
   ```python
   import pytest
   from tests.cli.conftest import cli_runner, sample_db_with_records
   from bagels.cli.records import records
   ```

2. **List command tests** (CLI-01, CLI-02):
   ```python
   def test_records_list_default(cli_runner, sample_db_with_records):
       """Test records list with default parameters."""
       result = cli_runner.invoke(records, ['list'])
       assert result.exit_code == 0
       assert result.output  # Placeholder assertion

   def test_records_list_month(cli_runner, sample_db_with_records):
       """Test records list with --month filter."""
       result = cli_runner.invoke(records, ['list', '--month', '2026-03'])
       assert result.exit_code == 0

   def test_records_filter_category(cli_runner, sample_db_with_records):
       """Test records list with --category filter."""
       result = cli_runner.invoke(records, ['list', '--category', 'Food'])
       assert result.exit_code == 0
   ```

3. **Show command tests** (CLI-03):
   ```python
   def test_records_show_valid(cli_runner, sample_db_with_records):
       """Test showing existing record."""
       result = cli_runner.invoke(records, ['show', 'r_001'])
       assert result.exit_code == 0
   ```

4. **Add command tests** (CLI-10):
   ```python
   def test_add_from_yaml(cli_runner, sample_db_with_records, tmp_path):
       """Test batch import from YAML."""
       # Placeholder for YAML import test
       assert True
   ```

5. **Format tests** (CLI-09):
   ```python
   def test_records_format_json(cli_runner, sample_db_with_records):
       """Test JSON output format."""
       result = cli_runner.invoke(records, ['list', '--format', 'json'])
       assert result.exit_code == 0
   ```

All tests should be placeholders with basic assertions. Implementation will come in Plan 02-02a.
  </action>
  <verify>
pytest tests/cli/test_records.py -v (tests discovered and importable, may fail on assertions)
  </verify>
  <done>Records test stubs created for CLI-01, CLI-02, CLI-03, CLI-10</done>
</task>

<task type="auto">
  <name>Task 3: Create summary and accounts test stubs</name>
  <files>tests/cli/test_summary.py, tests/cli/test_accounts.py</files>
  <action>
Create placeholder tests for summary and accounts commands:

**test_summary.py** (CLI-03):
```python
import pytest
from tests.cli.conftest import cli_runner, sample_db_with_records
from bagels.cli.summary import summary

def test_summary_default_month(cli_runner, sample_db_with_records):
    """Test summary for current month."""
    result = cli_runner.invoke(summary, [])
    assert result.exit_code == 0

def test_summary_specific_month(cli_runner, sample_db_with_records):
    """Test summary for specific month."""
    result = cli_runner.invoke(summary, ['--month', '2026-03'])
    assert result.exit_code == 0

def test_summary_json_format(cli_runner, sample_db_with_records):
    """Test JSON output format."""
    result = cli_runner.invoke(summary, ['--format', 'json'])
    assert result.exit_code == 0
```

**test_accounts.py** (CLI-04):
```python
import pytest
from tests.cli.conftest import cli_runner, sample_db_with_records
from bagels.cli.accounts import accounts

def test_accounts_list_default(cli_runner, sample_db_with_records):
    """Test accounts list with table format."""
    result = cli_runner.invoke(accounts, ['list'])
    assert result.exit_code == 0

def test_accounts_yaml_format(cli_runner, sample_db_with_records):
    """Test YAML output format (CLI-04 requirement)."""
    result = cli_runner.invoke(accounts, ['list', '--format', 'yaml'])
    assert result.exit_code == 0
```

All tests should be placeholders. Implementation will come in Plan 02-03.
  </action>
  <verify>
pytest tests/cli/test_summary.py tests/cli/test_accounts.py -v (tests discovered and importable)
  </verify>
  <done>Summary and accounts test stubs created for CLI-03, CLI-04</done>
</task>

<task type="auto">
  <name>Task 4: Create categories and spending test stubs</name>
  <files>tests/cli/test_categories.py, tests/cli/test_spending.py</files>
  <action>
Create placeholder tests for categories and spending commands:

**test_categories.py** (CLI-05):
```python
import pytest
from tests.cli.conftest import cli_runner, sample_db_with_records
from bagels.cli.categories import categories

def test_categories_tree(cli_runner, sample_db_with_records):
    """Test category tree display."""
    result = cli_runner.invoke(categories, ['tree'])
    assert result.exit_code == 0

def test_categories_tree_json(cli_runner, sample_db_with_records):
    """Test JSON output format."""
    result = cli_runner.invoke(categories, ['tree', '--format', 'json'])
    assert result.exit_code == 0
```

**test_spending.py** (CLI-06, CLI-07):
```python
import pytest
from tests.cli.conftest import cli_runner, sample_db_with_records
from bagels.cli.spending import spending

def test_spending_by_category(cli_runner, sample_db_with_records):
    """Test spending breakdown by category."""
    result = cli_runner.invoke(spending, ['by-category'])
    assert result.exit_code == 0

def test_spending_by_category_month(cli_runner, sample_db_with_records):
    """Test --month flag."""
    result = cli_runner.invoke(spending, ['by-category', '--month', '2026-03'])
    assert result.exit_code == 0

def test_spending_by_day(cli_runner, sample_db_with_records):
    """Test daily spending breakdown."""
    result = cli_runner.invoke(spending, ['by-day'])
    assert result.exit_code == 0

def test_spending_by_day_month(cli_runner, sample_db_with_records):
    """Test --month flag for daily spending."""
    result = cli_runner.invoke(spending, ['by-day', '--month', '2026-03'])
    assert result.exit_code == 0
```

All tests should be placeholders. Implementation will come in Plan 02-04.
  </action>
  <verify>
pytest tests/cli/test_categories.py tests/cli/test_spending.py -v (tests discovered and importable)
  </verify>
  <done>Categories and spending test stubs created for CLI-05, CLI-06, CLI-07</done>
</task>

<task type="auto">
  <name>Task 5: Create trends, llm, and schema test stubs</name>
  <files>tests/cli/test_trends.py, tests/cli/test_llm.py, tests/cli/test_schema.py</files>
  <action>
Create placeholder tests for trends, LLM, and schema commands:

**test_trends.py** (CLI-08):
```python
import pytest
from tests.cli.conftest import cli_runner, sample_db_with_records
from bagels.cli.trends import trends

def test_trends_default_months(cli_runner, sample_db_with_records):
    """Test 3-month comparison."""
    result = cli_runner.invoke(trends, [])
    assert result.exit_code == 0

def test_trends_category_filter(cli_runner, sample_db_with_records):
    """Test --category filter."""
    result = cli_runner.invoke(trends, ['--category', 'Food'])
    assert result.exit_code == 0
```

**test_llm.py** (LLM-01, LLM-04, LLM-05):
```python
import pytest
from tests.cli.conftest import cli_runner, sample_db_with_records
from bagels.cli.llm import llm

def test_llm_context_month(cli_runner, sample_db_with_records):
    """Test LLM context dump for current month."""
    result = cli_runner.invoke(llm, ['context'])
    assert result.exit_code == 0

def test_context_completeness(cli_runner, sample_db_with_records):
    """Test all sections present in context."""
    result = cli_runner.invoke(llm, ['context', '--month', '2026-03'])
    assert result.exit_code == 0
    # Placeholder: Check for accounts, summary, spending, records, budget keys

def test_context_budget_status(cli_runner, sample_db_with_records):
    """Test budget progress included in context."""
    result = cli_runner.invoke(llm, ['context'])
    assert result.exit_code == 0
```

**test_schema.py** (LLM-02, LLM-03):
```python
import pytest
from tests.cli.conftest import cli_runner, sample_db_with_records
from bagels.cli.schema import schema

def test_schema_full(cli_runner, sample_db_with_records):
    """Test full schema display."""
    result = cli_runner.invoke(schema, ['full'])
    assert result.exit_code == 0

def test_schema_model(cli_runner, sample_db_with_records):
    """Test model-specific schema."""
    result = cli_runner.invoke(schema, ['model', 'record'])
    assert result.exit_code == 0
```

All tests should be placeholders. Implementation will come in Plan 02-05.
  </action>
  <verify>
pytest tests/cli/test_trends.py tests/cli/test_llm.py tests/cli/test_schema.py -v (tests discovered and importable)
  </verify>
  <done>Trends, LLM, and schema test stubs created for CLI-08, LLM-01 through LLM-05</done>
</task>

<task type="auto">
  <name>Task 6: Create output formatters test stubs</name>
  <files>tests/cli/test_output_formatters.py</files>
  <action>
Create placeholder tests for output formatting (CLI-09):

**test_output_formatters.py**:
```python
import pytest
from tests.cli.conftest import sample_db_with_records, sample_records
from bagels.queries.formatters import format_records, to_json, to_yaml

def test_format_records_table(sample_records):
    """Test table format for records."""
    output = format_records(sample_records, output_format="table")
    assert output  # Placeholder assertion
    assert len(output) > 0

def test_format_records_json(sample_records):
    """Test JSON format for records."""
    output = format_records(sample_records, output_format="json")
    assert output
    # Placeholder: Validate JSON structure

def test_json_datetime_serialization(sample_records):
    """Test datetime objects serialize correctly."""
    output = to_json(sample_records)
    assert output
    # Placeholder: Check for datetime serialization errors

def test_parse_month_valid():
    """Test month parsing utility."""
    from bagels.queries.filters import parse_month
    result = parse_month("2026-03")
    assert result is not None  # Placeholder assertion

def test_parse_amount_range():
    """Test amount range parsing utility."""
    from bagels.queries.filters import parse_amount_range
    result = parse_amount_range("100..500")
    assert result is not None  # Placeholder assertion
```

All tests should be placeholders. Implementation will come in Plan 02-01.
  </action>
  <verify>
pytest tests/cli/test_output_formatters.py -v (tests discovered and importable)
  </verify>
  <done>Output formatter test stubs created for CLI-09</done>
</task>

<task type="auto">
  <name>Task 7: Update main conftest for CLI fixtures</name>
  <files>tests/conftest.py</files>
  <action>
Update main conftest.py to include CLI-specific fixtures if needed:

1. **Check if CLI fixtures needed at top level**:
   - Review tests/conftest.py for existing fixtures
   - Add any CLI-specific fixtures that all tests might need
   - Ensure no conflicts with existing fixtures

2. **Add CLI fixture imports if helpful**:
   ```python
   # Optional: Import CLI fixtures for convenience
   from tests.cli.conftest import cli_runner, sample_db_with_records
   ```

3. **Ensure pytest configuration**:
   - Verify pyproject.toml has pytest testpaths configured
   - Ensure tests/cli/ directory is discovered by pytest

This task may be minimal if existing conftest.py already has necessary fixtures.
  </action>
  <verify>
pytest tests/ -v (all tests discovered including new CLI tests)
python -c "import pytest; print(pytest.__version__)"
  </verify>
  <done>Main conftest updated to support CLI test infrastructure</done>
</task>

</tasks>

<verification>
1. pytest tests/cli/ -v discovers all 10 test files without errors
2. All test files import fixtures from tests/cli/conftest.py successfully
3. pytest tests/cli/test_records.py -v runs placeholder tests (may fail assertions)
4. pytest tests/cli/test_summary.py tests/cli/test_accounts.py -v runs
5. pytest tests/cli/test_categories.py tests/cli/test_spending.py -v runs
6. pytest tests/cli/test_trends.py tests/cli/test_llm.py tests/cli/test_schema.py -v runs
7. pytest tests/cli/test_output_formatters.py -v runs
8. All 11 Wave 0 requirements from VALIDATION.md are satisfied
</verification>

<success_criteria>
1. tests/cli/conftest.py created with shared fixtures (cli_runner, sample_db_with_records, etc.)
2. All 10 CLI test files created with placeholder tests
3. pytest can discover and import all CLI test files
4. All 15 requirements (CLI-01 through CLI-10, LLM-01 through LLM-05) have test stubs
5. Wave 1-3 plans can now reference test files in verify commands without Nyquist failures
</success_criteria>

<output>
After completion, create `.planning/phases/02-cli-query-layer/02-00-SUMMARY.md`
</output>
