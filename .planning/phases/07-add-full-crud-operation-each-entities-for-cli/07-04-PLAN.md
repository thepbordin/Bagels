---
phase: 07-add-full-crud-operation-each-entities-for-cli
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - SKILL.md
  - tests/cli/test_crud.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CRUD-01, CRUD-02, CRUD-03, CRUD-04, CRUD-05, CRUD-06]

must_haves:
  truths:
    - "SKILL.md documents all new CRUD commands for all 5 entities"
    - "Basic smoke tests verify all CRUD commands are registered and callable"
    - "All command groups importable from __main__ without errors"
    - "REQUIREMENTS.md defines CRUD-01 through CRUD-09 and includes them in the traceability table"
  artifacts:
    - path: "SKILL.md"
      provides: "Updated LLM reference with all CRUD mutation commands"
      contains: "bagels accounts add"
    - path: "tests/cli/test_crud.py"
      provides: "Smoke tests for CLI CRUD command registration"
      min_lines: 40
    - path: ".planning/REQUIREMENTS.md"
      provides: "CRUD-01 through CRUD-09 requirement definitions and traceability entries"
      contains: "CRUD-01"
  key_links:
    - from: "SKILL.md"
      to: "src/bagels/cli/*.py"
      via: "Documents all CLI commands"
      pattern: "bagels (accounts|categories|persons|records|templates) (add|show|update|delete)"
---

<objective>
Update SKILL.md with documentation for all new CRUD commands, create smoke tests verifying command registration, and define CRUD-01 through CRUD-09 requirements in REQUIREMENTS.md.

Purpose: Complete the phase by ensuring the LLM reference document covers all new mutation commands, basic tests confirm no import/registration errors, and requirements are formally tracked.

Output: Updated SKILL.md with full CRUD command reference. Smoke test file for CRUD command registration. REQUIREMENTS.md with CRUD-01 through CRUD-09 defined and traced to Phase 7.
</objective>

<execution_context>
@/Users/thepbordin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thepbordin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-add-full-crud-operation-each-entities-for-cli/07-CONTEXT.md
@.planning/phases/07-add-full-crud-operation-each-entities-for-cli/07-01-SUMMARY.md
@.planning/phases/07-add-full-crud-operation-each-entities-for-cli/07-02-SUMMARY.md
@.planning/phases/07-add-full-crud-operation-each-entities-for-cli/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md with CRUD command documentation</name>
  <files>SKILL.md</files>
  <read_first>
    - SKILL.md
    - src/bagels/cli/accounts.py
    - src/bagels/cli/categories.py
    - src/bagels/cli/persons.py
    - src/bagels/cli/templates.py
    - src/bagels/cli/records.py
    - src/bagels/__main__.py
  </read_first>
  <action>
Update `SKILL.md` to add documentation for all new CRUD mutation commands. Add a new section titled `## Mutation Commands — Entity CRUD` after the existing `## Mutation Commands` section (which currently only has `records add --from-yaml`).

Restructure the Mutation Commands section to include:

**Update the existing `### bagels records add` section** to document the new inline creation flags:
- Add a subsection showing inline creation with flags: `--label`, `--amount`, `--date`, `--account-id`, `--category-id`, `--person-id`, `--income`, `--transfer`, `--transfer-to-account-id`, `--format/-f`
- Note: When no `--yaml` is given, inline mode is used. Missing required fields are prompted interactively.
- Example: `bagels records add --label "Lunch" --amount 245 --date 2026-03-22 --account-id 1`

**Add `### bagels records update IDENTIFIER`:**
- Table of flags: `--label`, `--amount`, `--date`, `--account-id`, `--category-id`, `--person-id`, `--income/--no-income`, `--transfer/--no-transfer`, `--transfer-to-account-id`, `--format/-f`
- IDENTIFIER is integer ID or slug string
- Example: `bagels records update 42 --amount 300 --format json`
- Example: `bagels records update r_2026-03-22_001 --label "Updated label"`

**Add `### bagels records delete IDENTIFIER`:**
- Flags: `--force` (skip confirmation)
- Example: `bagels records delete 42 --force`

**Add `### bagels accounts add`:**
- Flags: `--name`, `--balance`, `--description`, `--hidden`, `--format/-f`
- Required: name, balance (prompted if missing)
- Example: `bagels accounts add --name "Savings" --balance 10000`

**Add `### bagels accounts show IDENTIFIER`:**
- `--format/-f`
- Example: `bagels accounts show 1 --format json`
- Example: `bagels accounts show acc_savings`

**Add `### bagels accounts update IDENTIFIER`:**
- Flags: `--name`, `--balance`, `--description`, `--hidden/--no-hidden`, `--format/-f`
- Example: `bagels accounts update 1 --name "New Savings"`

**Add `### bagels accounts delete IDENTIFIER`:**
- Flags: `--force`, `--cascade`
- Note: Soft delete. `--cascade` soft-deletes linked records.
- Example: `bagels accounts delete 1 --force`

**Add `### bagels categories list`:**
- `--format/-f`

**Add `### bagels categories show IDENTIFIER`:**
- `--format/-f`

**Add `### bagels categories add`:**
- Flags: `--name`, `--nature` (Want/Need/Must), `--color`, `--parent-id`, `--format/-f`
- Required: name, nature (prompted if missing)
- Example: `bagels categories add --name "Dining Out" --nature Need --parent-id 5`

**Add `### bagels categories update IDENTIFIER`:**
- Flags: `--name`, `--nature`, `--color`, `--parent-id`, `--format/-f`

**Add `### bagels categories delete IDENTIFIER`:**
- Flags: `--force`, `--cascade`
- Note: Also soft-deletes subcategories.

**Add `### bagels persons list`:**
- `--format/-f`

**Add `### bagels persons add`:**
- Flags: `--name`, `--format/-f`
- Required: name (prompted if missing)
- Example: `bagels persons add --name "Alice"`

**Add `### bagels persons show IDENTIFIER`:**
- `--format/-f`

**Add `### bagels persons update IDENTIFIER`:**
- Flags: `--name`, `--format/-f`

**Add `### bagels persons delete IDENTIFIER`:**
- Flags: `--force`, `--cascade`

**Add `### bagels templates list`:**
- `--format/-f`

**Add `### bagels templates add`:**
- Flags: `--label`, `--amount`, `--account-id`, `--category-id`, `--income`, `--transfer`, `--transfer-to-account-id`, `--format/-f`
- Required: label, amount, account-id (prompted if missing)
- Example: `bagels templates add --label "Rent" --amount 15000 --account-id 1`

**Add `### bagels templates show IDENTIFIER`:**
- `--format/-f`

**Add `### bagels templates update IDENTIFIER`:**
- Flags: `--label`, `--amount`, `--account-id`, `--category-id`, `--income/--no-income`, `--transfer/--no-transfer`, `--transfer-to-account-id`, `--format/-f`

**Add `### bagels templates delete IDENTIFIER`:**
- Flags: `--force`
- Note: Hard delete (templates are not soft-deleted).

**Update the Workflow Patterns section** to add pattern 5:

### 5. Create a Record from CLI Flags

**When:** Quickly add a single expense/income without YAML file.

```bash
bagels accounts list --format yaml    # get account IDs
bagels categories tree --format yaml  # get category IDs
bagels records add --label "Coffee" --amount 120 --date 2026-03-22 --account-id 1 --category-id 3
```

**Update the Tips section** to add:
- `IDENTIFIER` in CRUD commands accepts either integer ID or slug string.
- All create/update commands support `--format` for output format.
- Delete commands prompt for confirmation by default; use `--force` to skip.
- `--cascade` on delete also removes linked records (soft-delete).
  </action>
  <verify>
    <automated>cd /Users/thepbordin/Developer/Bagels && grep -c "bagels accounts add" SKILL.md && grep -c "bagels persons add" SKILL.md && grep -c "bagels categories add" SKILL.md && grep -c "bagels templates add" SKILL.md && grep -c "bagels records update" SKILL.md && grep -c "bagels records delete" SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains `bagels accounts add`
    - SKILL.md contains `bagels accounts show`
    - SKILL.md contains `bagels accounts update`
    - SKILL.md contains `bagels accounts delete`
    - SKILL.md contains `bagels categories add`
    - SKILL.md contains `bagels categories delete`
    - SKILL.md contains `bagels persons add`
    - SKILL.md contains `bagels persons delete`
    - SKILL.md contains `bagels templates add`
    - SKILL.md contains `bagels templates delete`
    - SKILL.md contains `bagels records update`
    - SKILL.md contains `bagels records delete`
    - SKILL.md contains `--force`
    - SKILL.md contains `--cascade`
    - SKILL.md contains the inline record creation example `bagels records add --label`
  </acceptance_criteria>
  <done>SKILL.md updated with complete CRUD command reference for all 5 entities including flags, examples, and workflow patterns.</done>
</task>

<task type="auto">
  <name>Task 2: Create smoke tests for CRUD command registration</name>
  <files>tests/cli/test_crud.py</files>
  <read_first>
    - tests/cli/test_main.py
    - src/bagels/cli/accounts.py
    - src/bagels/cli/categories.py
    - src/bagels/cli/persons.py
    - src/bagels/cli/templates.py
    - src/bagels/cli/records.py
    - src/bagels/__main__.py
  </read_first>
  <action>
Create `tests/cli/test_crud.py` with smoke tests that verify all CRUD commands are properly registered without requiring a database connection.

```python
"""Smoke tests for CLI CRUD command registration."""

import pytest


class TestAccountsCRUD:
    """Verify accounts command group has all CRUD subcommands."""

    def test_accounts_commands_registered(self):
        from bagels.cli.accounts import accounts
        cmd_names = [c.name for c in accounts.commands.values()]
        assert "list" in cmd_names
        assert "show" in cmd_names
        assert "add" in cmd_names
        assert "update" in cmd_names
        assert "delete" in cmd_names

    def test_accounts_add_has_name_option(self):
        from bagels.cli.accounts import accounts
        add_cmd = accounts.commands["add"]
        param_names = [p.name for p in add_cmd.params]
        assert "name" in param_names
        assert "balance" in param_names

    def test_accounts_delete_has_cascade(self):
        from bagels.cli.accounts import accounts
        delete_cmd = accounts.commands["delete"]
        param_names = [p.name for p in delete_cmd.params]
        assert "cascade" in param_names
        assert "force" in param_names


class TestCategoriesCRUD:
    """Verify categories command group has all CRUD subcommands."""

    def test_categories_commands_registered(self):
        from bagels.cli.categories import categories
        cmd_names = [c.name for c in categories.commands.values()]
        assert "tree" in cmd_names
        assert "list" in cmd_names
        assert "show" in cmd_names
        assert "add" in cmd_names
        assert "update" in cmd_names
        assert "delete" in cmd_names

    def test_categories_add_has_nature_option(self):
        from bagels.cli.categories import categories
        add_cmd = categories.commands["add"]
        param_names = [p.name for p in add_cmd.params]
        assert "name" in param_names
        assert "nature" in param_names

    def test_categories_delete_has_cascade(self):
        from bagels.cli.categories import categories
        delete_cmd = categories.commands["delete"]
        param_names = [p.name for p in delete_cmd.params]
        assert "cascade" in param_names
        assert "force" in param_names


class TestPersonsCRUD:
    """Verify persons command group has all CRUD subcommands."""

    def test_persons_commands_registered(self):
        from bagels.cli.persons import persons
        cmd_names = [c.name for c in persons.commands.values()]
        assert "list" in cmd_names
        assert "show" in cmd_names
        assert "add" in cmd_names
        assert "update" in cmd_names
        assert "delete" in cmd_names

    def test_persons_registered_in_cli(self):
        from bagels.__main__ import cli
        assert "persons" in cli.commands

    def test_persons_delete_has_cascade(self):
        from bagels.cli.persons import persons
        delete_cmd = persons.commands["delete"]
        param_names = [p.name for p in delete_cmd.params]
        assert "cascade" in param_names
        assert "force" in param_names


class TestTemplatesCRUD:
    """Verify templates command group has all CRUD subcommands."""

    def test_templates_commands_registered(self):
        from bagels.cli.templates import templates
        cmd_names = [c.name for c in templates.commands.values()]
        assert "list" in cmd_names
        assert "show" in cmd_names
        assert "add" in cmd_names
        assert "update" in cmd_names
        assert "delete" in cmd_names

    def test_templates_registered_in_cli(self):
        from bagels.__main__ import cli
        assert "templates" in cli.commands

    def test_templates_add_has_required_options(self):
        from bagels.cli.templates import templates
        add_cmd = templates.commands["add"]
        param_names = [p.name for p in add_cmd.params]
        assert "label" in param_names
        assert "amount" in param_names
        assert "account_id" in param_names


class TestRecordsCRUD:
    """Verify records command group has all CRUD subcommands."""

    def test_records_commands_registered(self):
        from bagels.cli.records import records
        cmd_names = [c.name for c in records.commands.values()]
        assert "list" in cmd_names
        assert "show" in cmd_names
        assert "add" in cmd_names
        assert "update" in cmd_names
        assert "delete" in cmd_names

    def test_records_add_has_inline_options(self):
        from bagels.cli.records import records
        add_cmd = records.commands["add"]
        param_names = [p.name for p in add_cmd.params]
        assert "yaml" in param_names
        assert "label" in param_names
        assert "amount" in param_names
        assert "date" in param_names
        assert "account_id" in param_names

    def test_records_update_has_identifier(self):
        from bagels.cli.records import records
        update_cmd = records.commands["update"]
        param_names = [p.name for p in update_cmd.params]
        assert "identifier" in param_names

    def test_records_delete_has_force(self):
        from bagels.cli.records import records
        delete_cmd = records.commands["delete"]
        param_names = [p.name for p in delete_cmd.params]
        assert "identifier" in param_names
        assert "force" in param_names
```

Write this test file exactly as shown above.
  </action>
  <verify>
    <automated>cd /Users/thepbordin/Developer/Bagels && uv run pytest tests/cli/test_crud.py -v --no-header 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/cli/test_crud.py exists
    - tests/cli/test_crud.py contains `class TestAccountsCRUD`
    - tests/cli/test_crud.py contains `class TestCategoriesCRUD`
    - tests/cli/test_crud.py contains `class TestPersonsCRUD`
    - tests/cli/test_crud.py contains `class TestTemplatesCRUD`
    - tests/cli/test_crud.py contains `class TestRecordsCRUD`
    - tests/cli/test_crud.py contains `assert "cascade" in param_names` for accounts, categories, and persons delete commands
    - `uv run pytest tests/cli/test_crud.py` passes with 0 failures
  </acceptance_criteria>
  <done>Smoke tests pass confirming all CRUD commands are properly registered across all 5 entity CLI modules, including cascade param assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Define CRUD-01 through CRUD-09 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/07-add-full-crud-operation-each-entities-for-cli/07-CONTEXT.md
  </read_first>
  <action>
Update `.planning/REQUIREMENTS.md` to define the CRUD-01 through CRUD-09 requirements referenced in Phase 7 plans, and add them to the traceability table.

**Add a new section** after the `## LLM CLI Documentation (Phase 6)` section and before `## Out of Scope`:

```markdown
## CLI CRUD Mutations (Phase 7)

- [ ] **CRUD-01**: `bagels accounts add/show/update/delete IDENTIFIER` commands with --force and --cascade flags
- [ ] **CRUD-02**: `bagels categories add/list/show/update/delete IDENTIFIER` commands with --force and --cascade flags
- [ ] **CRUD-03**: `bagels persons list/add/show/update/delete IDENTIFIER` commands registered as new top-level group
- [ ] **CRUD-04**: `bagels templates list/add/show/update/delete IDENTIFIER` commands registered as new top-level group
- [ ] **CRUD-05**: `bagels records add` supports inline flag-based single record creation (--label, --amount, --date, --account-id) in addition to batch --yaml import
- [ ] **CRUD-06**: `bagels records update/delete IDENTIFIER` commands with --force on delete
- [ ] **CRUD-07**: All CRUD create/update commands support --format/-f (table/json/yaml) and echo the affected entity
- [ ] **CRUD-08**: Delete commands show confirmation prompt by default; --force skips prompt; --cascade soft-deletes linked records (hard delete for records, which is an intentional exception)
- [ ] **CRUD-09**: IDENTIFIER argument on update/show/delete accepts integer ID or slug string
```

**Add entries to the traceability table** (append to the existing table before the coverage summary line):

```
| CRUD-01 | Phase 7 | Planned |
| CRUD-02 | Phase 7 | Planned |
| CRUD-03 | Phase 7 | Planned |
| CRUD-04 | Phase 7 | Planned |
| CRUD-05 | Phase 7 | Planned |
| CRUD-06 | Phase 7 | Planned |
| CRUD-07 | Phase 7 | Planned |
| CRUD-08 | Phase 7 | Planned |
| CRUD-09 | Phase 7 | Planned |
```

**Update the coverage summary** to reflect the new total (was 60 requirements, add 9 → 69):

```
**Coverage:**
- Total tracked requirements (including reduction, documentation, and CRUD mutations): 69
- Mapped to phases: 69 (100%)
- Unmapped: 0
```

**Update the "Last updated" line** at the bottom:
```
*Last updated: 2026-03-22 for Phase 7 CLI CRUD mutations*
```
  </action>
  <verify>
    <automated>cd /Users/thepbordin/Developer/Bagels && grep -c "CRUD-0" .planning/REQUIREMENTS.md && grep "CRUD-09" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/REQUIREMENTS.md contains `**CRUD-01**`
    - .planning/REQUIREMENTS.md contains `**CRUD-09**`
    - .planning/REQUIREMENTS.md contains `## CLI CRUD Mutations (Phase 7)`
    - .planning/REQUIREMENTS.md traceability table contains `| CRUD-01 | Phase 7 | Planned |`
    - .planning/REQUIREMENTS.md traceability table contains `| CRUD-09 | Phase 7 | Planned |`
    - .planning/REQUIREMENTS.md coverage total updated to 69
  </acceptance_criteria>
  <done>REQUIREMENTS.md formally defines CRUD-01 through CRUD-09 with descriptions matching the locked decisions in CONTEXT.md, and all 9 are traced to Phase 7 in the traceability table.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/cli/test_crud.py -v` passes all tests
- `grep -c "bagels accounts add" SKILL.md` returns >= 1
- `grep -c "bagels records delete" SKILL.md` returns >= 1
- SKILL.md contains documentation for all 5 entity CRUD commands
- `grep "CRUD-09" .planning/REQUIREMENTS.md` returns a match
</verification>

<success_criteria>
- SKILL.md fully documents all new CRUD commands with flags, examples, and workflow patterns
- All smoke tests pass confirming command registration, including --cascade param assertions for accounts/categories/persons delete
- REQUIREMENTS.md formally defines CRUD-01 through CRUD-09 and traces them to Phase 7
- Phase 07 is complete: all 5 entities have full CLI CRUD
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-full-crud-operation-each-entities-for-cli/07-04-SUMMARY.md`
</output>
